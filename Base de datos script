create database prueba;
use prueba;

create table usuario (id int unsigned auto_increment primary key, nombre varchar(255), correo varchar(255));

insert into usuario(nombre, correo) values ('Jeisson', 'jei@gmail.com');

select*from usuario;
